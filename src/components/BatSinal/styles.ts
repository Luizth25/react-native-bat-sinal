import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  button: {
    borderWidth: 1,
    borderColor: "#FFFF00",
    backgroundColor: "#000",
    padding: 20,
    borderRadius: 8,
  },
  text: {
    color: "#fff",
    textAlign: "center",
    fontSize: 20,
  },
});
