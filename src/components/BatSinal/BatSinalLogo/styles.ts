import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  image: {
    width: 250,
    height: 250,
    marginBottom: 40,
  },
  Button: {
    borderWidth: 1,
    borderColor: "#FFFF00",
    backgroundColor: "#000",
    padding: 24,
    borderRadius: 8,
  },
  text: {
    color: "#fff",
    textAlign: "center",
    fontSize: 20,
  },
});
