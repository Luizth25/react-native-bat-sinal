export type TDataFrom = {
  email?: string;
  name: string;
  phone: string;
  observation?: string;
  localization: string;
};

export type TBatSinalFormProps = {
  desactive: React.Dispatch<React.SetStateAction<boolean>>;
};
